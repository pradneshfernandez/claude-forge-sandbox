---
id: T-007
title: Scaffold todo-cli package skeleton and test-package marker
status: done
wave: 4
depends_on: []
owns:
  - examples/todo-cli/README.md
  - examples/todo-cli/tests/__init__.py
model: haiku
security-review: false
tests: []
user-actions: []
verify: "test -f examples/todo-cli/README.md && test -f examples/todo-cli/tests/__init__.py"
---
## Objective
The directory `examples/todo-cli/` exists with a short README describing how to run
the app and its tests, and `examples/todo-cli/tests/__init__.py` exists (empty) so that
`python3 -m unittest discover -s examples/todo-cli -p 'test_*.py'` can descend into the
`tests/` subdirectory as a package. This is the interface-first scaffold that the two
test-writers (test_core.py, test_storage.py) and the CLI task depend on.

## Context (read these, nothing else)
- SPEC.md §Toolchain (lines 102–113) — exact test/lint/run commands
- SPEC.md §Architecture (lines 46–62) — three-module layout under examples/todo-cli/

## Instructions
1. Create the file `examples/todo-cli/tests/__init__.py` with EMPTY content (zero bytes
   is acceptable; a single trailing newline is fine). Its only purpose is to make
   `tests/` an importable package for unittest discovery.
2. Create `examples/todo-cli/README.md` containing ONLY:
   - A one-line title `# todo-cli`.
   - A one-sentence description: a stdlib-only single-user CLI todo manager storing
     tasks in `~/.todo-cli/todos.json` (overridable via `TODO_FILE`).
   - A "Run" section with the exact line: `python3 examples/todo-cli/todo.py <subcommand> [args]`
   - A "Test" section with the exact line:
     `python3 -m unittest discover -s examples/todo-cli -p 'test_*.py'`
   - A "Lint" section with the exact line:
     `python3 -m py_compile examples/todo-cli/core.py examples/todo-cli/storage.py examples/todo-cli/todo.py`
3. Do NOT create core.py, storage.py, todo.py, or any test_*.py — other tasks own those.

## Acceptance criteria (each must be mechanically checkable)
- [ ] `examples/todo-cli/tests/__init__.py` exists and is empty (≤ 1 byte or a lone newline)
- [ ] `examples/todo-cli/README.md` exists and contains the exact Run, Test, and Lint commands above
- [ ] No other files under examples/todo-cli/ were created

## Non-goals — do NOT
- Do NOT create core.py, storage.py, todo.py (owned by T-008, T-009, T-010)
- Do NOT create any test_*.py file (owned by the test-writers)
- Do NOT add any third-party dependency or config file

## Model reason (required if model != haiku)
—
