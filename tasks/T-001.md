---
id: T-001
title: Verify policy hooks fire correctly
status: done
wave: 1
depends_on: []
owns:
  - .claude/audit/
model: haiku
security-review: false
tests: []
user-actions: []
verify: "test -x .claude/hooks/audit-log.sh && test -x .claude/hooks/protect-paths.sh && test -x .claude/hooks/block-secret-strings.sh"
---
## Objective
Confirm all three PreToolUse hooks execute and behave as designed before any real work runs.

## Context (read these, nothing else)
- .claude/hooks/*.sh
- .claude/settings.json (hooks section)

## Instructions
1. Run any allowed command (e.g. `git status`) and confirm a line was appended to
   .claude/audit/tool-calls.jsonl.
2. Attempt to edit .claude/settings.json via the Edit tool — confirm the
   protect-paths hook BLOCKS it (this block is the success condition).
3. Echo a fake key shape `sk-abcdefghijklmnopqrstuv` in a Bash command — confirm
   block-secret-strings blocks it.

## Acceptance criteria
- [x] Audit log exists and grows per tool call
- [x] Protected-path edit is blocked with the policy message
- [x] Credential-shaped command is blocked

## Non-goals — do NOT
- Do NOT modify any hook or settings file (they are protected; humans edit them).
