---
id: T-002
title: Tailor permission allow-list to the project toolchain
status: done
wave: 1
depends_on: []
owns: []
model: haiku
security-review: true
tests: []
user-actions: [UA-1]
verify: "python3 -c \"import json; json.load(open('.claude/settings.json'))\""
---
## Objective
settings.json allow-list matches the real toolchain (e.g. pnpm/cargo/pytest) so the
user isn't prompt-fatigued into over-approving. Deny-list is never weakened.

## Context
- .claude/settings.json
- SPEC.md §Toolchain (once /plan has run)

## Instructions
1. PROPOSE (do not apply — path is hook-protected) an updated allow list covering
   the project's test/lint/build commands only. Present as a diff for the user to
   apply manually (UA-1).
2. Never move anything out of `deny`. Never add `WebFetch` or `curl` to allow.

## Acceptance criteria
- [x] User confirmed no diff needed (no toolchain exists in this repo yet); JSON validates
- [x] Deny list unchanged

## Non-goals — do NOT
- Do NOT edit settings.json directly. Do NOT suggest --dangerously-skip-permissions.

## Model reason
haiku fine; security-review true because it touches permission policy.
