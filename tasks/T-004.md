---
id: T-004
title: Pipeline dry run on a toy spec
status: pending
wave: 2
depends_on: [T-001, T-002]
owns:
  - SPEC.md
  - docs/adr/
  - tasks/
  - docs/STATE.md
model: sonnet
security-review: false
tests: []
user-actions: []
verify: "test -f SPEC.md && ls tasks/ | grep -c '^T-' | awk '{exit ($1<4)}'"
---
## Objective
Prove the full loop works end-to-end: /plan → /decompose → /execute-wave → /review
→ /handoff on a deliberately tiny project ("CLI todo app, JSON file storage, no deps").

## Instructions
1. Run /plan with the toy prompt; keep the interview to ≤ 5 questions.
2. Run /decompose; confirm ≥ 2 tasks land in the same wave with disjoint owns.
3. Run /execute-wave, /review, /handoff.
4. Note every friction point in the handoff's Gotchas section.

## Acceptance criteria
- [ ] SPEC.md, ≥4 tasks, ≥1 parallel wave, tests written by test-writer
- [ ] At least one intentionally missing detail (e.g. "where to store the file?")
      produced a QUESTION or USER_ACTIONS item — not a guess
- [ ] Handoff entry written; STATE.md consistent with task frontmatter

## Model reason
Cross-cutting orchestration judgment → sonnet.
